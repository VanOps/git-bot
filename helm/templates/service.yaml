apiVersion: v1
kind: Service
metadata:
  name: {{ include "git-bot.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "git-bot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "git-bot.selectorLabels" . | nindent 4 }}
