apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: git-bot
  labels:
    app.kubernetes.io/name: grafana
    app.kubernetes.io/component: visualization
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: grafana
    app.kubernetes.io/component: visualization
  ports:
    - name: http
      port: 3001
      targetPort: http
      protocol: TCP
