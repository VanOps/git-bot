apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana-data
  namespace: git-bot
  labels:
    app.kubernetes.io/name: grafana
    app.kubernetes.io/component: visualization
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
