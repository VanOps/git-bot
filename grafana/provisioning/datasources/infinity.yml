apiVersion: 1

datasources:
  - name: Infinity – DORA API
    type: yesoreyeram-infinity-datasource
    uid: infinity-dora
    access: proxy
    isDefault: true
    editable: false
    jsonData:
      # Hosts permitidos (obligatorio en Infinity v2.x para requests en modo proxy)
      allowedHosts:
        - http://probot:3000
